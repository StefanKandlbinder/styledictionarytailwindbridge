module.exports = {primary: {
              100:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-100), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-100), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-100))`
                },
              200:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-200), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-200), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-200))`
                },
              300:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-300), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-300), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-300))`
                },
              400:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-400), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-400), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-400))`
                },
              500:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-500), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-500), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-500))`
                },
              600:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-600), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-600), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-600))`
                },
              700:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-700), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-700), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-700))`
                },
              800:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-800), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-800), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-800))`
                },
              900:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-primary-900), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-primary-900), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-primary-900))`
                },},accent: {
              100:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-100), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-100), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-100))`
                },
              200:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-200), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-200), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-200))`
                },
              300:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-300), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-300), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-300))`
                },
              400:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-400), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-400), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-400))`
                },
              500:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-500), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-500), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-500))`
                },
              600:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-600), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-600), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-600))`
                },
              700:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-700), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-700), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-700))`
                },
              800:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-800), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-800), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-800))`
                },
              900:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-accent-900), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-accent-900), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-accent-900))`
                },},warning: {
              100:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-100), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-100), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-100))`
                },
              200:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-200), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-200), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-200))`
                },
              300:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-300), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-300), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-300))`
                },
              400:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-400), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-400), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-400))`
                },
              500:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-500), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-500), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-500))`
                },
              600:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-600), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-600), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-600))`
                },
              700:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-700), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-700), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-700))`
                },
              800:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-800), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-800), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-800))`
                },
              900:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-warning-900), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-warning-900), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-warning-900))`
                },},gray: {
              100:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-100), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-100), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-100))`
                },
              200:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-200), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-200), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-200))`
                },
              300:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-300), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-300), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-300))`
                },
              400:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-400), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-400), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-400))`
                },
              500:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-500), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-500), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-500))`
                },
              600:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-600), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-600), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-600))`
                },
              700:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-700), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-700), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-700))`
                },
              800:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-800), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-800), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-800))`
                },
              900:
                ({ opacityVariable, opacityValue }) => {
                  if (opacityValue !== undefined) {
                    return `rgba(var(--one-color-gray-900), ${opacityValue})`
                  }
                  if (opacityVariable !== undefined) {
                    return `rgba(var(--one-color-gray-900), ${opacityVariable})`
                  }
                    return `rgba(var(--one-color-gray-900))`
                },},}